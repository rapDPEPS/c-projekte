#ifndef READFUNCTIONS_H
#define READFUNCTIONS_H

//Takes the user Input and tries to Convert it to an Integer. Garbage input will be discarded.
void readNumber(int* dest);

//Takes the user Input and tries to Convert it to a Float. Garbage input will be discarded
void readFloat(float* dest);

#endif